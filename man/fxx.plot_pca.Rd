% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fxx.plot_pca.R
\name{fxx.plot_pca}
\alias{fxx.plot_pca}
\title{PCA scatter plot helper with multiple styles}
\usage{
fxx.plot_pca(
  pca_data,
  group = NULL,
  x_col = "PC1",
  y_col = "PC2",
  mycol = NULL
)
}
\arguments{
\item{pca_data}{data.frame containing PCA coordinates and optional group column.}

\item{group}{character, grouping column name. If NULL, auto-detect a suitable column.}

\item{x_col}{character, x-axis column name. Default "PC1".}

\item{y_col}{character, y-axis column name. Default "PC2".}

\item{mycol}{character vector of colors. If NULL, generated automatically.}
}
\value{
A named list of ggplot objects.
}
\description{
PCA scatter plot helper with multiple styles
}
\examples{
set.seed(11)
n <- 30; p <- 20
mat <- matrix(rnorm(n * p), nrow = p, ncol = n)
grp <- rep(c("QC", "Pg", "AE"), length.out = n)
mat_t <- t(mat)

pca_out <- stats::prcomp(mat_t, scale. = TRUE)
pca_data <- data.frame(
  PC1 = pca_out$x[, 1],
  PC2 = pca_out$x[, 2],
  Group = grp
)
cols <- c("#27AE60", "#8F5F9F", "#2E86AB")
pl <- fxx.plot_pca(pca_data, group = "Group", mycol = cols)
pl$colored
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fxx.plot_umap.R
\name{fxx.plot_umap}
\alias{fxx.plot_umap}
\title{UMAP scatter plot helper with multiple styles}
\usage{
fxx.plot_umap(
  umap_data,
  group = NULL,
  x_col = "UMAP1",
  y_col = "UMAP2",
  mycol = NULL
)
}
\arguments{
\item{umap_data}{data.frame containing UMAP coordinates and optional group column.}

\item{group}{character, grouping column name. If NULL, auto-detect a suitable column.}

\item{x_col}{character, x-axis column name. Default "UMAP1".}

\item{y_col}{character, y-axis column name. Default "UMAP2".}

\item{mycol}{character vector of colors. If NULL, generated automatically.}
}
\value{
A named list of ggplot objects.
}
\description{
UMAP scatter plot helper with multiple styles
}
\examples{
if (requireNamespace("umap", quietly = TRUE)) {
  set.seed(11)
  n <- 30; p <- 20
  mat <- matrix(rnorm(n * p), nrow = p, ncol = n)
  grp <- rep(c("QC", "Pg", "AE"), length.out = n)
  mat_t <- t(mat)

  cfg <- umap::umap.defaults
  cfg$n_neighbors <- max(2, min(10, nrow(mat_t) - 1))
  umap_out <- umap::umap(mat_t, config = cfg)

  umap_data <- data.frame(
    UMAP1 = umap_out$layout[, 1],
    UMAP2 = umap_out$layout[, 2],
    Group = grp
  )
  cols <- c("#27AE60", "#8F5F9F", "#2E86AB")
  pl <- fxx.plot_umap(umap_data, group = "Group", mycol = cols)
  pl$colored
}
}
